import Node from './node';

class BinaryTree {
    constructor(){
        this.root = null;
    }

    // insert(data)
    // remove(data)
    // findMinNode()
    // getRootNode()
    // inorder(node)
    // preorder(node)
    // postoder(node)
    // search(node, data)
}